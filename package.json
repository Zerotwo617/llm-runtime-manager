{
  "name": "llm-runtime-manager",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "ui"
  ],
  "scripts": {
    "dev": "npm run dev --prefix ui",
    "build": "npm run build --prefix ui",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.7"
  }
}
